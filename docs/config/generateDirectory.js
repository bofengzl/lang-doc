/**
 * 动态生成目录结构
 * @param {String} type 类 
 */
const generateDirectory = (type) => {
  readFileName();
  return [];
}

/**
 * 读取指定文件夹下目录名称
 */
const readFileName = () => {
  // const moduleState = import.meta.glob('./*.js');
  // console.log("🚀 ~ file: generateDirectory.js:14 ~ readFileName ~ moduleState:", moduleState)
  // const moduleSync = import.meta.globEager('./*.js');
  // console.log("🚀 ~ file: generateDirectory.js:16 ~ readFileName ~ moduleSync:", moduleSync)
}